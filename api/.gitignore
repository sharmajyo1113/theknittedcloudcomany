node_modules
# Keep environment variables out of version control
.env

/generated/prisma

# Prisma CLI's auto-scaffolded AI-agent reference docs (from `prisma init`) —
# tool-vendor documentation, not project code.
/.agents
/.claude
/.windsurf
skills-lock.json
